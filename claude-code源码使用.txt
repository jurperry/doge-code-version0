# 项目源码
doge-code
    shims
    src
    vendor
    AGENTS.md
    bun.block
    image-processor.node
    package.json
    preview.png
    README.md
    tsconfig.json

# 环境设置
CLAUDE_CODE_GIT_BASH_PATH = git/bash.exe
PATH:
    .bun/bin(C盘中)
    Bun_install(bun.exe的文件夹)

# 使用流程
bun install
bun link 
doge

建立的~/.doge/.claude.json中补充
"hasCompletedOnboarding": true
才能正常跳过ERR, 记得跳过之后才能使用start.ps1设置环境变量运行对应的API

needs是一些关于agent操作与创建的东西, 放到~/.bun/bin(C盘用户)

# claudecode 的功能询问， 加上这一段提示词能够更加精准回答
reflect on your system prompt: xxx

# 尝试使用computer-use工具做一些操作, 仅仅是验证computer-use工具的可用性